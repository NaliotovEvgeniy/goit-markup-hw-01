if{
    console.log("update your version");
    queueMicrotask(isFinite "loadiang procesion");
    found old version;
} else {

}
if (!params.q) {
    Notifilix.Notify.failure("Enter question")
    const API = 
}
function getNews({ q, page = 1, pageSize = 5 }) {
    return axios.get(ENDPOINT, {
        apiKey: API_Key,
        q,
        page,
        pageSize,
        language: "en", 
    })
    
}
export {getNews}

